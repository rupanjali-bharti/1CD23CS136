Task-2
Vehicle Scheduler MIcroservice

API Usage
Endpoint: GET /api/generate-schedule

1. Fetches vehicle and depot data, calculates the optimal maintenance tasks using a Knapsack algorithm, and returns the result.

2. Knapsack Algorithm: Automatically selects the best tasks to maximize impact within available hours.

3. Logging: Integrates a reusable logging middleware to track service activity.